import{FieldValue}from'firebase-admin/firestore';import{adminDb}from'@/lib/firebaseAdmin';import{requireAdmin}from'@/lib/staffAccess';export const runtime='nodejs';export const dynamic='force-dynamic';
const COURSE_TAGS=['spring_course','summer_course','winter_course','past_exam','exam_private','exam_recommendation','exam_general'];
const PRESETS=['垂水中','福田中','模試受験',...COURSE_TAGS];
export async function GET(request){try{await requireAdmin(request);const[users,elementary,parents]=await Promise.all([adminDb.collection('users').get(),adminDb.collection('adminStudents').get(),adminDb.collection('parentAccounts').get()]);const students=[...users.docs.map(doc=>{const value=doc.data();return{id:doc.id,name:value.realName||value.displayName||'名前未設定',tags:[...(value.tags||[]),...(value.courseTags||[])]}}),...elementary.docs.map(doc=>{const value=doc.data();return{id:`elementary_${doc.id}`,name:value.name||value.realName||'名前未設定',tags:value.tags||[]}})].sort((a,b)=>a.name.localeCompare(b.name,'ja'));return Response.json({presets:PRESETS,courseTags:COURSE_TAGS,students,parents:parents.docs.map(doc=>({id:doc.id,name:doc.data().displayName||'保護者',tags:doc.data().tags||[]})).sort((a,b)=>a.name.localeCompare(b.name,'ja'))});}catch(error){return Response.json({error:error.message},{status:error.status||500})}}
export async function POST(request){try{const admin=await requireAdmin(request),body=await request.json(),isParent=body.target==='parents',tag=String(body.tag||'').trim().slice(0,40),ids=[...new Set(body.ids||[])].filter(id=>/^[A-Za-z0-9_-]{6,128}$/.test(id)).slice(0,200);if(!tag||!ids.length)throw new Error('タグと対象者を選択してください。');if(isParent&&COURSE_TAGS.includes(tag))throw new Error('講習・入試タグは生徒だけに設定できます。');if(COURSE_TAGS.includes(tag)&&ids.some(id=>id.startsWith('elementary_')))throw new Error('講習・入試タグはアカウント生徒だけに設定できます。');const field=!isParent&&COURSE_TAGS.includes(tag)?'courseTags':'tags',batch=adminDb.batch(),now=FieldValue.serverTimestamp();ids.forEach(id=>{const elementary=!isParent&&id.startsWith('elementary_'),collection=isParent?'parentAccounts':elementary?'adminStudents':'users',docId=elementary?id.slice(11):id;batch.set(adminDb.collection(collection).doc(docId),{[field]:body.enabled===false?FieldValue.arrayRemove(tag):FieldValue.arrayUnion(tag),updatedBy:admin.uid,updatedAt:now},{merge:true})});await batch.commit();return Response.json({saved:true,count:ids.length});}catch(error){return Response.json({error:error.message},{status:error.status||400})}}
