import { FieldValue } from 'firebase-admin/firestore';
import { adminDb } from '@/lib/firebaseAdmin';
import { requireAdmin } from '@/lib/staffAccess';
export const dynamic='force-dynamic';
const validTerm=value=>/^20\d{2}_[123]$/.test(value||'');
export async function GET(request){try{await requireAdmin(request);const termId=new URL(request.url).searchParams.get('term');if(!validTerm(termId))throw new Error('学期を確認してください。');const [year,term]=termId.split('_'),users=await adminDb.collection('users').get();const students=users.docs.map(doc=>({id:doc.id,...doc.data()})).filter(item=>item.active!==false&&item.enrollmentStatus!=='withdrawn'&&Number(item.grade)>=7&&Number(item.grade)<=9);const rows=await Promise.all(students.map(async student=>{const [scores,status]=await Promise.all([adminDb.collection('users').doc(student.id).collection('scores').where('year','==',year).where('term','==',`${term}学期`).get(),adminDb.collection('scoreSubmissionTerms').doc(termId).collection('students').doc(student.id).get()]);const hasExam=scores.docs.some(doc=>doc.data().type==='exam'),hasInternal=scores.docs.some(doc=>doc.data().type==='internal'),saved=status.data()||{};return{uid:student.id,name:student.realName||student.displayName||'名前未設定',grade:Number(student.grade),examReceived:saved.examReceived===true,internalReceived:saved.internalReceived===true,hasExamData:hasExam,hasInternalData:hasInternal}}));return Response.json({termId,students:rows.sort((a,b)=>a.grade-b.grade||a.name.localeCompare(b.name,'ja'))});}catch(error){return Response.json({error:error.message},{status:error.status||400})}}
export async function POST(request){try{const admin=await requireAdmin(request),body=await request.json();if(!validTerm(body.termId)||!/^[A-Za-z0-9_-]{1,128}$/.test(body.uid||''))throw new Error('対象を確認してください。');await adminDb.collection('scoreSubmissionTerms').doc(body.termId).collection('students').doc(body.uid).set({uid:body.uid,termId:body.termId,examReceived:body.examReceived===true,internalReceived:body.internalReceived===true,updatedBy:admin.uid,updatedAt:FieldValue.serverTimestamp()},{merge:true});return Response.json({saved:true});}catch(error){return Response.json({error:error.message},{status:error.status||400})}}
