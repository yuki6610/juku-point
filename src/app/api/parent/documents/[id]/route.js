import { adminDb,adminStorage } from '@/lib/firebaseAdmin';
import { requireParent,linkedChildren } from '@/lib/parentAccess';
import { normalizeStudentKey } from '@/lib/staffAccess';
const visible=(item,grade,key)=>item.targetType==='all'||!item.targetType||item.targetType==='elementary'&&grade<=6||item.targetType==='middle'&&grade>=7&&grade<=9||item.targetType==='grade'&&String(grade)===String(item.targetValue)||item.targetType==='student'&&String(item.targetValue||'').split(',').map(value=>value.trim()).some(value=>value===key||value===key.replace(/^(user|elementary)_/,''));
export async function GET(request,{params}){try{const parent=await requireParent(request),query=new URL(request.url).searchParams,key=normalizeStudentKey(query.get('student'));if(!(await linkedChildren(parent.uid,parent.role==='admin')).includes(key))return new Response('Forbidden',{status:403});const elementary=key.startsWith('elementary_'),student=await adminDb.collection(elementary?'adminStudents':'users').doc(key.replace(/^(user|elementary)_/,'')).get(),portal=await adminDb.collection('admin_data').doc('parentPortal').get(),item=(portal.data()?.documents||[]).find(value=>String(value.id)===String(params.id)),today=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Tokyo'}).format(new Date());if(!student.exists||!/^parentDocuments\/[A-Za-z0-9-]+\.pdf$/.test(item?.storagePath||'')||!visible(item,Number(student.data().grade),key)||(item.startDate&&item.startDate>today)||(item.endDate&&item.endDate<today))return new Response('Not found',{status:404});const [buffer]=await adminStorage.bucket().file(item.storagePath).download();return new Response(buffer,{headers:{'Content-Type':'application/pdf','Content-Disposition':`inline; filename="document.pdf"`,'Cache-Control':'private,max-age=300'}});}catch(error){return new Response(error.message,{status:error.status||400})}}
