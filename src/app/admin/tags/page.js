'use client';import{useEffect,useMemo,useState}from'react';import{auth}from'@/firebaseConfig';import'./tags.css';const LABELS={spring_course:'春期講習',summer_course:'夏期講習',winter_course:'冬期講習',past_exam:'公立過去問',exam_private:'私立入試',exam_recommendation:'公立推薦',exam_general:'公立一般'};async function api(options={}){const response=await fetch('/api/admin/tags',{...options,headers:{'Content-Type':'application/json',Authorization:`Bearer ${await auth.currentUser?.getIdToken()}`}}),data=await response.json();if(!response.ok)throw new Error(data.error);return data}export default function Tags(){const[data,setData]=useState(null),[target,setTarget]=useState('students'),[tag,setTag]=useState('垂水中'),[ids,setIds]=useState([]),[notice,setNotice]=useState(''),[busy,setBusy]=useState(false);const load=()=>api().then(setData).catch(error=>setNotice(error.message));useEffect(()=>{load()},[]);useEffect(()=>setIds([]),[target,tag]);const available=(data?.presets||[]).filter(value=>target==='students'||!(data?.courseTags||[]).includes(value));useEffect(()=>{if(available.length&&!available.includes(tag))setTag(available[0])},[target,data]);const rows=useMemo(()=>data?.[target]||[],[data,target]),save=async enabled=>{setBusy(true);try{await api({method:'POST',body:JSON.stringify({target,tag,ids,enabled})});setNotice(`${ids.length}人のタグを更新しました。`);setIds([]);await load()}catch(error){setNotice(error.message)}finally{setBusy(false)}};return <main className="tag-admin"><header><small>GROUP TAGS</small><h1>タグ一括管理</h1><p>学校・講習・入試を含む生徒タグと保護者タグは、この画面からまとめて付与・解除します。</p></header>{notice&&<p role="status">{notice}</p>}<div className="tag-controls"><select value={target} onChange={e=>setTarget(e.target.value)}><option value="students">生徒</option><option value="parents">保護者</option></select><select value={tag} onChange={e=>setTag(e.target.value)}>{available.map(value=><option key={value} value={value}>{LABELS[value]||value}</option>)}</select><input aria-label="任意タグ" placeholder="任意タグを直接入力" onBlur={e=>e.target.value.trim()&&setTag(e.target.value.trim())}/></div><div className="tag-people">{rows.map(row=><label className={row.tags.includes(tag)?'has-tag':''} key={row.id}><input type="checkbox" checked={ids.includes(row.id)} onChange={()=>setIds(old=>old.includes(row.id)?old.filter(id=>id!==row.id):[...old,row.id])}/><span><strong>{row.name}</strong><small>{row.tags.includes(tag)?`${LABELS[tag]||tag} 付与済み`:'未付与'}</small></span></label>)}</div><footer><span>{ids.length}人選択中</span><button disabled={busy||!ids.length} onClick={()=>save(true)}>まとめて付与</button><button disabled={busy||!ids.length} onClick={()=>save(false)}>まとめて解除</button></footer></main>}
