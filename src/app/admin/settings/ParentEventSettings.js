'use client';
import { useEffect,useState } from 'react';
import { auth } from '@/firebaseConfig';
import './parent-events.css';
const today=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Tokyo'}).format(new Date());
const blank=()=>({id:crypto.randomUUID(),name:'',type:'classroom',startDate:today(),endDate:today(),startTime:'',endTime:'',targetType:'all',targetValue:'',showParent:true,showStudent:false});
export default function ParentEventSettings(){const[items,setItems]=useState([]),[notice,setNotice]=useState(''),[busy,setBusy]=useState('');const api=async(url,options={})=>{const response=await fetch(url,{...options,headers:{'Content-Type':'application/json',Authorization:`Bearer ${await auth.currentUser?.getIdToken()}`}}),data=await response.json();if(!response.ok)throw new Error(data.error);return data};const load=()=>api('/api/admin/parent-events').then(data=>setItems(data.items)).catch(error=>setNotice(error.message));useEffect(()=>{load()},[]);const update=(id,field,value)=>setItems(old=>old.map(item=>item.id===id?{...item,[field]:value}:item));const save=async item=>{setBusy(item.id);try{await api('/api/admin/parent-events',{method:'POST',body:JSON.stringify(item)});setNotice(`${item.name}を保存しました。`);await load()}catch(error){setNotice(error.message)}finally{setBusy('')}};const remove=async item=>{if(!confirm(`${item.name||'この予定'}を削除しますか？`))return;setBusy(item.id);try{await api(`/api/admin/parent-events?id=${encodeURIComponent(item.id)}`,{method:'DELETE'});setNotice('予定を削除しました。');await load()}catch(error){setNotice(error.message)}finally{setBusy('')}};return <section className="event-settings"><header><span>FAMILY CALENDAR</span><h2>保護者カレンダーの予定</h2><p>既存の通常授業・振替・宿題確認日は自動表示されます。学校行事や面談などだけ登録してください。</p></header>{notice&&<p role="status">{notice}</p>}<button onClick={()=>setItems(old=>[blank(),...old])}>＋ 予定を追加</button><div className="event-admin-list">{items.map(item=><article key={item.id}><input placeholder="イベント名" value={item.name||''} onChange={e=>update(item.id,'name',e.target.value)}/><select value={item.type||'other'} onChange={e=>update(item.id,'type',e.target.value)}><option value="school">学校</option><option value="classroom">教室</option><option value="interview">面談</option><option value="exam">入試・模試</option><option value="course">講習</option><option value="lesson">授業</option><option value="other">その他</option></select><label>開始<input type="date" value={item.startDate||''} onChange={e=>update(item.id,'startDate',e.target.value)}/></label><label>終了<input type="date" value={item.endDate||item.startDate||''} onChange={e=>update(item.id,'endDate',e.target.value)}/></label><label>開始時刻<input type="time" value={item.startTime||''} onChange={e=>update(item.id,'startTime',e.target.value)}/></label><label>終了時刻<input type="time" value={item.endTime||''} onChange={e=>update(item.id,'endTime',e.target.value)}/></label><select value={item.targetType||'all'} onChange={e=>update(item.id,'targetType',e.target.value)}><option value="all">全員</option><option value="grade">学年</option><option value="school">学校</option><option value="student">個別生徒</option><option value="parentTag">保護者タグ</option></select>{item.targetType!=='all'&&<input placeholder={item.targetType==='parentTag'?'タグ名':'対象'} value={item.targetValue||''} onChange={e=>update(item.id,'targetValue',e.target.value)}/>}<label><input type="checkbox" checked={item.showParent!==false} onChange={e=>update(item.id,'showParent',e.target.checked)}/>保護者へ表示</label><label><input type="checkbox" checked={item.showStudent===true} onChange={e=>update(item.id,'showStudent',e.target.checked)}/>生徒へ表示</label><div><button disabled={busy===item.id} onClick={()=>save(item)}>個別保存</button><button disabled={busy===item.id} onClick={()=>remove(item)}>削除</button></div></article>)}</div></section>}
