'use client';
import { useEffect,useState } from 'react';
import { auth } from '@/firebaseConfig';
const api=async(options={})=>{const response=await fetch('/api/admin/lesson-reports',{...options,headers:{'Content-Type':'application/json',Authorization:`Bearer ${await auth.currentUser?.getIdToken()}`}}),data=await response.json();if(!response.ok)throw new Error(data.error);return data};
export default function LessonReportApprovals(){const[items,setItems]=useState([]),[notice,setNotice]=useState(''),[busy,setBusy]=useState('');const load=()=>api().then(data=>setItems(data.items||[])).catch(error=>setNotice(error.message));useEffect(()=>{load()},[]);const update=(id,text)=>setItems(old=>old.map(item=>`${item.studentKey}_${item.date}`===id?{...item,text}:item));const approve=async item=>{const id=`${item.studentKey}_${item.date}`;setBusy(id);try{await api({method:'PATCH',body:JSON.stringify({studentKey:item.studentKey,date:item.date,text:item.text})});setItems(old=>old.filter(row=>`${row.studentKey}_${row.date}`!==id));setNotice('授業報告を承認し、保護者ページへ公開しました。')}catch(error){setNotice(error.message)}finally{setBusy('')}};return <section className="lesson-report-approvals"><header><div><small>REPORT APPROVAL</small><h2>未承認の授業報告</h2></div><strong>{items.length}件</strong></header>{notice&&<p role="status">{notice}</p>}{items.map(item=>{const id=`${item.studentKey}_${item.date}`;return <article key={id}><div><b>{item.name}</b><time>{item.date}</time></div><textarea rows="6" maxLength="2000" value={item.text||''} onChange={event=>update(id,event.target.value)}/><button disabled={busy===id||!item.text?.trim()} onClick={()=>approve(item)}>編集内容を承認・公開</button></article>})}{!items.length&&<p>未承認の授業報告はありません。</p>}</section>}
