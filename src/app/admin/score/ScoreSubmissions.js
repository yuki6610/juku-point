'use client';
import { useEffect,useState } from 'react';
import { auth } from '@/firebaseConfig';
import { useAcademicContext } from '@/lib/useAcademicContext';
const gradeLabel=value=>`中${Number(value)-6}`;
export default function ScoreSubmissions(){const academic=useAcademicContext(),[termId,setTermId]=useState(''),[rows,setRows]=useState([]),[notice,setNotice]=useState(''),[busy,setBusy]=useState(false);const terms=academic.settings.flatMap(item=>[1,2,3].map(term=>({id:`${item.year}_${term}`,label:`${item.year}年度 ${term}学期`}))).sort((a,b)=>b.id.localeCompare(a.id));useEffect(()=>{if(!termId&&academic.current)setTermId(academic.current.id)},[academic.current,termId]);const api=async(path,options={})=>{const token=await auth.currentUser?.getIdToken();const response=await fetch(path,{...options,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`}});const result=await response.json();if(!response.ok)throw new Error(result.error);return result};const load=()=>{if(!termId)return;setBusy(true);api(`/api/admin/score-submissions?term=${termId}`).then(result=>setRows(result.students)).catch(error=>setNotice(error.message)).finally(()=>setBusy(false))};useEffect(load,[termId]);const change=async(row,field,value)=>{const next={...row,[field]:value};setRows(old=>old.map(item=>item.uid===row.uid?next:item));try{await api('/api/admin/score-submissions',{method:'POST',body:JSON.stringify({uid:row.uid,termId,examReceived:next.examReceived,internalReceived:next.internalReceived})});setNotice(`${row.name}さんの提出状況を保存しました。`)}catch(error){setRows(old=>old.map(item=>item.uid===row.uid?row:item));setNotice(error.message)}};return <section className="score-submissions"><div className="submission-heading"><div><h2>テスト・通知表の提出状況</h2><p>未提出にすると、保護者画面へ持参のお願いを表示します。</p></div><select value={termId} onChange={e=>setTermId(e.target.value)}>{terms.map(item=><option key={item.id} value={item.id}>{item.label}</option>)}</select></div>{notice&&<p role="status">{notice}</p>}{busy?<p>読み込み中…</p>:<div className="submission-table"><header><span>生徒</span><span>定期テスト</span><span>通知表</span></header>{rows.map(row=><div key={row.uid}><span><strong>{row.name}</strong><small>{gradeLabel(row.grade)}</small></span><label><input type="checkbox" checked={row.examReceived} onChange={e=>change(row,'examReceived',e.target.checked)}/>{row.examReceived?'提出済み':'未提出'}{row.hasExamData&&<small>成績入力あり</small>}</label><label><input type="checkbox" checked={row.internalReceived} onChange={e=>change(row,'internalReceived',e.target.checked)}/>{row.internalReceived?'提出済み':'未提出'}{row.hasInternalData&&<small>成績入力あり</small>}</label></div>)}</div>}</section>}
