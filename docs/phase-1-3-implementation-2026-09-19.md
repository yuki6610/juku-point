# Phase 1〜3 実装記録

実施日: 2026-09-19  
対象: `/Users/tanakayuuki/Desktop/juku-point`

## 実装済み

- 生徒の交換履歴を旧配列、新サブコレクション、日付欠損文書から自動併読するよう変更。
- 自習中一覧をクライアントの全生徒N+1読取から管理者APIへ移行。
- 強制退出も管理者APIのトランザクションへ移行。
- 志望校判定の単語テスト表示を、選択学期の総正答数・総問題数による平均正答率へ修正。累計値への誤ったフォールバックを廃止。
- 講師スマホ画面の横幅収縮を抑止する幅・包含CSSを追加。
- 保護者招待URLを `APP_BASE_URL` から生成し、内部Originを招待URLに使用しないよう変更。
- 講師の新規招待発行を停止し、本人申請→管理者承認方式を追加。
- 生徒ログイン画面から「管理者」の表示を削除。管理者のログイン判定自体は維持。
- 学習記録の年度・学期選択時に、設定済み学期期間と授業日を同期。
- 固定2026年の旧学期設定を削除。稼働中の保存処理は共通学期設定を使用。
- 旧出欠ページを統合済み学習記録の出欠タブへリダイレクト。
- 未保存状態のサイドバー遷移処理で、Element以外のイベント対象による例外を防止。
- 既存の振替互換読取、取消マーカー、旧形式優先順位は維持。破壊的なDB移行は行っていない。

## データ保護

- 既存コレクションの削除、名称変更、一括更新は実施していない。
- 旧講師招待はAPI側の読取・確定互換を残し、新規発行だけを保護者に限定した。
- 交換履歴の旧配列は削除せず、表示時に重複排除する。
- 出欠の旧保存先は削除せず、既存互換処理を継続する。

## 新規データ

- `pendingTeachers/{uid}`: 講師本人の承認待ち申請。
- 承認時に既存の `teachers/{uid}` へ移し、申請文書は状態を `approved` にする。

## 検証結果

- `next build`: 成功。
- Nodeテスト: 74件すべて成功。
- Firestore本番データの変更: なし。
- デプロイ: 未実施。

## デプロイ前の注意

- `apphosting.yaml` に `APP_BASE_URL` を追加している。
- `firestore.rules` に `pendingTeachers` の管理者専用ルールを追加している。
- アプリのGitデプロイに加えて、Firestore Rulesのデプロイが必要。
